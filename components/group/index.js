const {GroupFactory} = require('./GroupFactory');
const {GroupWidget} = require('./GroupWidget');
const {GroupModel} = require('./GroupModel');

module.exports = {GroupFactory, GroupWidget, GroupModel};
